# P3采集平台实施说明

> 日期：2026-07-27  
> 当前批次：P3第一轮代码实施 Batch 1  
> 状态：实施中

## 1. 本批次完成内容

本批次建立：

```text
Python 3.11项目骨架
FastAPI基础服务
配置与密钥边界
PostgreSQL连接
Alembic迁移
meta数据目录表
ops任务系统表
首批10个Tushare RAW表
Task幂等键
SKIP LOCKED任务领取基础能力
Lease + lease_token防旧Worker回写
Scheduler advisory lock基础能力
/health /ready /version
```

## 2. RawBatch设计补齐

详细设计链路一直包含：

```text
CollectRun -> RawBatch -> RAW
```

并且RAW通用字段包含 `_raw_batch_id`，但P3阶段门槛复核文件中的“第一轮数据库实施范围”漏列了RawBatch物理表。

本批次按既有架构补充：

```text
raw.raw_batch
```

这不是增加新架构，而是补齐已经确认的追溯链路。

## 3. 当前数据库对象

```text
meta.data_source
meta.data_item
meta.source_binding

ops.task_definition
ops.collect_task
ops.collect_run
ops.request_slice
ops.slice_attempt
ops.worker_registry
ops.scheduler_state
ops.data_watermark
ops.task_checkpoint
ops.rate_limit_state
ops.circuit_breaker_state

raw.raw_batch
raw.tushare_trade_cal
raw.tushare_stock_basic
raw.tushare_daily
raw.tushare_adj_factor
raw.tushare_daily_basic
raw.tushare_suspend_d
raw.tushare_stk_limit
raw.tushare_stk_mins
raw.tushare_income
raw.tushare_fina_indicator
```

## 4. 初始目录数据

Alembic第二个迁移初始化：

- Tushare；
- AKShare；
- legacy_quantstock；
- 第一批10个DataItem；
- 对应Tushare SourceBinding。

所有Binding初始：

```text
capability_status = unknown
```

真实权限必须由后续CapabilityProbe确认。

## 5. 当前尚未完成

本批次没有宣称完成真实采集。

下一批必须实现：

```text
TushareAdapter
CapabilityProbe
RateLimiter
首批Slice Planner
Worker执行链
trade_calendar真实采集
stock_basic真实采集
stock_daily真实采集
```

因此P3状态仍为：

```text
实施中
```
