# 第2步 数据底座实施记录

> Work Item: WI-0001 (feature_spec / requirement_change_path)
> Spec Version: PSV-0001 → PSV-0002 (merged)
> Branch: `feature/2-s2-001-s2-025-wi-0001`
> Base commit: c93cf6e

## 1. 迁移文件

| 版本 | 文件 | 内容 |
|------|------|------|
| 0013 | `migrations/versions/0013_lineage_and_snapshot.py` | lineage schema + lineage_edge 表 + clean.data_snapshot/data_snapshot_input 表 + READY 不可变触发器 |
| 0014 | `migrations/versions/0014_clean_published_at_financial_dataitem.py` | 10 CLEAN 表 published_at + financial_income/financial_indicator 多版本表 + DataItem 元数据种子 |
| 0015 | `migrations/versions/0015_audit_runcheck_datagap_rawevidence.py` | AuditEvent 扩展+只追加触发器 + run_type CHECK + DataGap VERIFIED + RAW content_hash/fetched_at/schema_fingerprint |

迁移链: `0012_p4_minute_governance` → 0013 → 0014 → 0015 (head)

## 2. 新增代码模块

### 2.1 Lineage 血缘 (P3 扩展)
- `app/storage/models/lineage.py` — LineageEdge ORM 模型
- `app/lineage/service.py` — 扩展 write_edge() + traverse_lineage() (递归 CTE)

### 2.2 DataContext 统一查询 (P5)
- `app/datacontext/__init__.py`
- `app/datacontext/context.py` — DataContext 主类
- `app/datacontext/query.py` — QueryContext, Frequency, AdjustmentMethod, QualityPolicy, TimeMode, SecurityScope 值对象
- `app/datacontext/time_semantics.py` — resolve_cutoff, TimeMode 三模式
- `app/datacontext/adjustment.py` — apply_adjustment 动态复权
- `app/datacontext/alignment.py` — 多频率对齐
- `app/datacontext/snapshot_builder.py` — DataSnapshot 构建 + content_fingerprint
- `app/datacontext/readers/daily.py` — 日线读取器
- `app/datacontext/readers/minute.py` — 分钟读取器
- `app/datacontext/readers/financial.py` — 财务读取器
- `app/datacontext/readers/event.py` — 事件读取器

### 2.3 DataSnapshot ORM
- `app/storage/models/snapshot.py` — DataSnapshot + DataSnapshotInput

### 2.4 统一查询 API
- `app/api/routes/data.py` — /api/v1/data/{daily,minute,financial,events,calendar,securities}
- `app/api/schemas/data.py` — DataResponse, DataSemantics, 请求模型

### 2.5 采集与治理强化
- `app/core/config.py` — worker_lost_threshold_seconds, recovery_sla_seconds, query_timeout_seconds
- `app/collect/scheduler.py` — recover_lost_workers()
- `app/catalog/bootstrap.py` — validate_dataitem_metadata(), DataItemMetadataError
- `app/collect/idempotency.py` — check_idempotency_or_force(), ForceRerunRequired

### 2.6 运维脚本
- `compose.test.yml` — server-test 独立环境 (端口 15432/18001)
- `.env.test.example`
- `scripts/db_migrate_disk/` — precheck.sh, migrate.sh, rollback.sh, README.md
- `scripts/db_backup/` — full_backup.sh, manifest.json, README.md
- `scripts/db_restore/` — restore.sh, verify.sh, README.md
- `scripts/minute_archive/` — compress_policy.sql, archive.sh, README.md

### 2.7 配置
- `.importlinter` — DataContext 禁止依赖 raw 模型的静态检查

## 3. 测试套件

| 目录 | 内容 |
|------|------|
| `tests/conftest.py` | PG session fixture, skip_no_pg marker |
| `tests/integration/` | 状态机、幂等、RAW 证据、CLEAN 版本、质量门禁、DataGap、API 超时、运维查询、DataContext、Snapshot 不可变、性能 |
| `tests/lineage/` | lineage_edge 写入与递归查询 |
| `tests/idempotency/` | force_rerun 路径 |
| `tests/anti_lookahead/` | 6 类防未来测试 |
| `tests/contract/` | API 契约测试 |
| `tests/backup_restore/` | 备份恢复校验 |
| `tests/fault_recovery/` | server-test 隔离、迁盘预检 |
| `tests/alembic/` | 空库升级、现有库升级 |
| `tests/e2e/` | 10 DataItem × 8 阶段矩阵 |

## 4. 验证状态

| 检查项 | 状态 | 说明 |
|--------|------|------|
| py_compile 全部 .py | PASS | 所有新建/修改文件语法正确 |
| import-linter 配置 | PASS | .importlinter 存在 |
| 真实 PG/TimescaleDB 迁移 | WAITING_USER_EXECUTION | 需在 server-test 环境执行 `alembic upgrade head` |
| 集成测试 | WAITING_USER_EXECUTION | 需真实 PG 环境运行 pytest |
| 端到端矩阵 | WAITING_USER_EXECUTION | 需真实 PG + 数据 |

## 5. 已知限制

- Python 3.13 环境与 pyproject.toml 要求 >=3.11,<3.12 不匹配（需用户在正确环境验证）
- psycopg2-binary 而非 psycopg3（项目 pyproject.toml 要求 psycopg[binary]>=3.2）
- StockPoolVersion 为 single-scope 占位（第3步实现完整版本）
- stable 数据库操作全部标记 WAITING_USER_EXECUTION
