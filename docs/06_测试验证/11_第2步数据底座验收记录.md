# 第2步 数据底座验收记录

> Work Item: WI-0001
> 验收日期: 2026-07-29
> 验收环境: server-test（PostgreSQL 16.14 + TimescaleDB 2.28.3，独立数据库，与 stable 完全隔离）

## 1. 代码级验证 (PASS)

### 1.1 Python 语法验证
所有新建和修改的 .py 文件均通过 `python -m py_compile` 语法检查。

验证范围:
- 3 个迁移文件 (0013-0015)
- lineage 模型和服务
- DataContext 全部 12 个模块文件
- API 路由和 schema
- 采集/治理强化代码
- 全部测试文件

### 1.2 文件完整性验证
所有 tasks.md 声明的文件均已创建，目录结构与设计规格一致。

### 1.3 配置验证
- .importlinter 存在，配置 DataContext 不依赖 raw 模型
- compose.test.yml 存在，端口/卷/project 隔离
- .env.test.example 存在

## 2. 数据库级验证 (PASS)

以下验证已在 server-test（PostgreSQL 16.14 + TimescaleDB 2.28.3）真实环境执行并通过:

### 2.1 迁移验证
```bash
# 在 server-test 环境
export QUANTSTOCK1_TEST_DATABASE_URL="postgresql+psycopg://quantstock1_test:change_me@localhost:15432/quantstock1_test"
alembic upgrade head
```

### 2.2 集成测试
```bash
python -m pytest tests/integration/ tests/lineage/ tests/idempotency/ tests/anti_lookahead/ tests/contract/ -v
```

### 2.3 端到端矩阵
```bash
python -m pytest tests/e2e/ -v
python -m pytest tests/alembic/ -v
```

## 3. 已知限制

| 项目 | 结果 | 说明 |
|------|------|------|
| server-test 真实迁移 | PASS | alembic upgrade head 通过，head=0015_audit_gap_rawev |
| 迁移升降级 | PASS | base→0015 升级、0015→0012 降级、0012→0015 重新升级全部通过 |
| 集成/端到端测试 | PASS | 290 passed、0 failed、0 skipped |
| 备份 SHA256 校验 | PASS | 全量备份校验通过 |
| 恢复到独立数据库 | PASS | 恢复并校验通过 |
| stable 环境操作 | WAITING_USER_EXECUTION | 由用户手动执行，不在第2步收尾范围 |

## 4. 验收结论

| 维度 | 结论 |
|------|------|
| 代码完整性 | PASS — 所有文件已创建，语法正确 |
| 架构合规性 | PASS — DataContext 不读 RAW，API 有超时，响应含元数据 |
| 数据库迁移 | PASS — server-test 真实 PG，head=0015，升降级循环通过 |
| 集成测试 | PASS — server-test 真实 PG，全部通过 |
| 端到端验收 | PASS — server-test 真实 PG + 数据，290 passed/0 failed/0 skipped |
| 备份恢复 | PASS — 全量备份 + SHA256 + 独立库恢复通过 |
| SpecForge 治理 | verification_gate/formal_version_gate/close_gate 通过，semantic_closure_valid=true；WI-0001 停在 verification_done（SpecForge 工具缺陷，见 DEC-032/DEC-033），不视为 quantStock1 未通过 |

## 5. 第2步验收结论

第2步 P3—P5 数据底座在 server-test 真实环境（PostgreSQL 16.14 + TimescaleDB 2.28.3）通过全部验收：290 passed、0 failed、0 skipped；迁移 base→0015 升级、0015→0012 降级、0012→0015 重新升级全部通过；全量备份 + SHA256 校验 + 恢复到独立数据库全部通过。

业务代码基线固化为 d40ca76。WI-0001 因 SpecForge sf_close_gate 工具缺陷（改写审计后用旧 SHA256 校验产生 SEMANTIC_CLOSURE_INPUT_STALE 循环）停在 verification_done，该缺陷在 SpecForge 项目单独修复，不视为 quantStock1 第2步未通过。
