# 第3步研究与回测验收计划

> 基线：`b0463bee7df45a4795b9e013d053ef19fd95afe2`
> 任务：S3-001—S3-016
> 状态：已确认，等待执行

## 1. P0阻断项

任一项失败，第3步不能通过：

- 正式版本可以被覆盖修改；
- Feature或回测读取RAW；
- Feature或策略输入无法追溯到DataSnapshot；
- 历史股票池使用当前成分回填；
- 策略以eval、exec或数据库裸脚本运行；
- API同步执行长回测；
- 同一收盘价同时产生信号和成交；
- T+1、停牌或涨跌停规则未验证；
- 费用和交易规则无版本或无生效日期；
- 资金、持仓和净值无法对账；
- 回测可以读取未来available_at数据；
- 同输入重复回测结果不一致；
- 真实数据库测试存在跳过；
- 端到端只使用玩具数据；
- stable环境被未经批准修改。

## 2. A组：迁移和对象

验证：

- 空库升级到新head；
- 0015升级到新head；
- 新Schema和表；
- 外键；
- 唯一约束；
- 检查约束；
- 索引；
- READY/ACTIVE版本不可变；
- downgrade或前向修复路径。

## 3. B组：Feature

验证：

- Definition和Version分离；
- 版本绑定实现Digest和代码提交；
- 依赖可追溯；
- Run和ResultSet分离；
- 增量；
- 重算；
- 幂等；
- available_at传播；
- 质量状态传播；
- 8个公共Feature；
- 真实数据数值抽样核对；
- 不读取RAW；
- 不一Feature一表。

## 4. C组：Analysis

验证：

- 四层对象；
- 标签与Feature输入隔离；
- 覆盖率；
- 排名和标准化；
- IC/RankIC；
- 分组收益；
- 稳定性和换手；
- 结果可解释；
- 结果可追溯；
- 不把未来收益标签泄漏给策略。

## 5. D组：StockPool

验证：

- Definition和Version分离；
- Version不可变；
- 历史成员；
- 上市、退市、停牌和风险状态；
- 历史流动性；
- 相同输入重复生成一致；
- 当前成分回填历史数量为0。

## 6. E组：Strategy和ResearchTask

验证：

- Definition、Implementation、Version分离；
- 代码入口受控；
- 无eval/exec；
- 参数Schema；
- 参数快照；
- Feature依赖绑定；
- 股票池绑定；
- ResearchTask持久化；
- Worker角色隔离；
- Lease、Heartbeat、LOST、Retry、Cancel；
- API只返回task_id和run_id。

## 7. F组：交易规则和撮合

验证至少覆盖：

- 交易日；
- T+1；
- 停牌；
- 涨停买入；
- 跌停卖出；
- 非锁死涨跌停Bar；
- 无涨跌幅阶段；
- 主板、科创板、创业板、北交所规则选择；
- 风险警示状态；
- 上市阶段；
- 生效日期切换；
- 交易单位；
- 现金不足；
- 持仓不足；
- NEXT_OPEN；
- NEXT_CLOSE；
- 信号时间早于成交时间。

## 8. G组：成本和账务

验证：

- 佣金；
- 最低佣金；
- 印花税；
- 过户费；
- 滑点；
- 买入资金扣减；
- 卖出回款；
- T+1可卖数量；
- PositionLot；
- 现金不为负；
- 持仓不凭空产生；
- 总资产=现金+持仓市值；
- 净值变化可解释；
- 每日账务平衡。

## 9. H组：BacktestRun和复现

验证：

- 全部输入不可变绑定；
- Run指纹；
- DataSnapshot；
- FeatureResultSet；
- StockPoolVersion；
- StrategyVersion；
- 参数快照；
- MarketRuleVersion；
- CostModelVersion；
- FillPolicyVersion；
- code_commit；
- engine_version；
- random_seed；
- 同输入重复结果一致；
- 任一输入变化生成新Run；
- 结果不覆盖。

## 10. I组：策略比较

验证统一：

- 起止日期；
- 初始资金；
- 基准；
- DataSnapshot；
- 股票池或差异说明；
- 成本；
- 交易规则；
- FillPolicy；
- 指标定义；
- 年化参数。

不同口径不得被系统默认为可直接比较。

## 11. J组：真实端到端

最少完成：

```text
真实A股日线
不少于3年
8个公共Feature
5类分析
历史股票池
1个示例策略
至少2个参数版本
2次同输入复现
1次策略比较
```

保存：

- 数据覆盖；
- 证券数量；
- Feature行数；
- 订单数；
- 成交数；
- 拒单数；
- 回测交易日数；
- 初始和最终资产；
- 对账误差；
- 指标；
- 输入指纹；
- 运行时间；
- 内存峰值。

## 12. K组：性能

记录：

- Feature吞吐；
- Analysis耗时；
- 股票池耗时；
- 回测耗时；
- 数据库写入量；
- ResultSet大小；
- API查询p50/p95；
- Research Worker内存峰值。

按P2.4目标判定。无目标项只记录基线，不伪造结论。

## 13. L组：回归和恢复

验证：

- 第2步290项真实测试保持通过；
- 新增测试全部通过；
- 0失败；
- 数据库测试0跳过；
- API健康；
- server-test隔离；
- 备份恢复回归；
- stable未修改。

## 14. 证据模板

每个用例记录：

```text
用例ID
关联S3任务
环境
提交
迁移版本
数据范围
输入指纹
命令
期望
实际
日志
结论
```

## 15. 最终结论

只允许：

```text
PASS
FAIL
BLOCKED
```

策略收益高低不得影响PASS。
